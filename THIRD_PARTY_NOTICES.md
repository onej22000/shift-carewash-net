# Third-Party Notices

CareWash Shift & Operations uses third-party open-source packages installed through Composer.

Third-party components remain subject to their own licenses. The repository license does not replace or override those licenses.

## mPDF

The current `composer.lock` records:

- Package: `mpdf/mpdf`
- Version: `v8.3.1`
- Declared license: `GPL-2.0-only`

mPDF also depends on additional packages with their own licenses.

Before redistributing a combined application, review all package licenses and the obligations that apply to your distribution model.

You can inspect the installed Composer license information with:

```bash
composer licenses
```

Also review `composer.lock` for the exact dependency versions used by your checkout.

## Brand and trademark assets

Some deployments or historical project assets may contain organization, service, or store branding.

Software source-code licensing does not automatically grant rights to use third-party trademarks, brand names, or logos. If you deploy or redistribute this project, replace brand-specific assets unless you have permission to use them.
